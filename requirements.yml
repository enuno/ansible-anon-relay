# Ansible Galaxy requirements for Anon Relay Deployment
collections:
  - name: community.docker
    version: ">=3.0.0"
  - name: ansible.posix
    version: ">=1.4.0"
  - name: community.general
    version: ">=5.0.0"
# Python requirements for Docker modules
dependencies:
  - docker>=5.0.0
  - docker-compose>=1.29.0
