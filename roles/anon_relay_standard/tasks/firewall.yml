# Firewall configuration for standard relay
- name: Install UFW
  package:
    name: ufw
    state: present
  tags:
    - anon
    - standard
    - firewall
    - ufw
- name: Reset UFW to defaults
  ufw:
    state: reset
  tags:
    - anon
    - standard
    - firewall
    - ufw
- name: Set UFW default policies
  ufw:
    direction: "{{ item.direction }}"
    policy: "{{ item.policy }}"
  loop:
    - {direction: "incoming", policy: "deny"}
    - {direction: "outgoing", policy: "allow"}
  tags:
    - anon
    - standard
    - firewall
    - ufw
- name: Allow SSH access
  ufw:
    rule: allow
    port: "{{ anon_ssh_port }}"
    proto: tcp
    comment: "SSH access"
  tags:
    - anon
    - standard
    - firewall
    - ufw
- name: Allow ORPort access
  ufw:
    rule: allow
    port: "{{ anon_relay_or_port }}"
    proto: tcp
    comment: "ORPort for relay traffic"
  tags:
    - anon
    - standard
    - firewall
    - ufw
- name: Allow IPv6 ORPort access
  ufw:
    rule: allow
    port: "{{ anon_relay_or_port }}"
    proto: tcp
    comment: "IPv6 ORPort for relay traffic"
  when: anon_ipv6_enabled | default(false)
  tags:
    - anon
    - standard
    - firewall
    - ufw
    - ipv6
- name: Enable UFW
  ufw:
    state: enabled
  tags:
    - anon
    - standard
    - firewall
    - ufw
- name: Display UFW status
  command: ufw status verbose
  register: ufw_status
  changed_when: false
  tags:
    - anon
    - standard
    - firewall
    - ufw
- name: Show UFW status
  debug:
    msg: "{{ ufw_status.stdout_lines }}"
  tags:
    - anon
    - standard
    - firewall
    - ufw
